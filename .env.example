# Copie para .env — nunca commitar o .env real (lição CarToken: secrets fora do repo).
DATABASE_URL=postgres://ditofeito:senha@localhost:5432/ditofeito
POSTGRES_PASSWORD=senha
NODE_ENV=development
PORT=3000
